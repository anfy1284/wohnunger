node_modules
.env
.DS_Store
*.sqlite
dbSettings.json
dbSettings.sqlite.json
dbSettings.postgres.json
_dbSettings.postgres.json
database.sqlite-journal
tmp/
ai_settings.json

# Резервное копирование: настройки этой машины (каталог, публичный ключ, хэш API-ключа)
# и сами копии. В репозиторий не попадают ни при каких условиях — дампы содержат
# персональные данные клиентов.
backupSettings.json
backups/

# Режим обслуживания: флаг живёт на конкретной машине и переживает перезапуск
# процесса — в репозиторий он попасть не должен ни при каких обстоятельствах,
# иначе развёрнутая из репозитория копия поднимется заблокированной.
maintenance.flag.json
maintenance.flag.json.tmp-*
maintenance.log
backups_wohnungerDb/
