'use strict';

/**
 * Обработчики проведения денежных документов.
 *
 * ФАЙЛ ГРУЗЯТ ДВА ПРОЦЕССА — главный (форма, команды) и форкнутый воркер
 * планировщика, который и выполняет проведение. Поэтому модуль обязан быть
 * ЧИСТОЙ ФАБРИКОЙ без побочных эффектов: ни регистраций, ни подключений, ни
 * чтения настроек на уровне модуля. Ровно то же правило и по той же причине
 * действует для `scheduler.handlers.js`.
 *
 * Регистрация в `init.js` через `entityHooks.register` для проведения НЕ
 * ГОДИТСЯ: `init.js` выполняет только главный процесс, и в воркере обработчика
 * не существует — проведение падает с «обработчик не зарегистрирован».
 * Подробности: `drive_root/db/postingHandlers.js`.
 *
 * Сами функции живут в `hooks/posting.js` — здесь только объявление имён.
 */

const posting = require('./hooks/posting');

module.exports = function (modelsDB, Utilities) {
    return {
        'cash.postReceipt': posting.postReceipt,
        'cash.postPayment': posting.postPayment,
        'cash.postTransfer': posting.postTransfer,
        // Проверочный документ каскада (ТЗ §11.6): единственный, кто читает
        // регистр при проведении, и потому единственный, на ком каскад
        // перепроведения вообще проверяем.
        'cash.postCheck': posting.postCheck
    };
};
